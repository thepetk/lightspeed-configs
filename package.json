{
  "name": "formatter-tools",
  "private": true,
  "devDependencies": {
    "prettier": "^3.2.0"
  },
  "scripts": {
    "format": "prettier --write 'images.yaml' 'lightspeed-core-configs/**/*.{yaml,yml}' 'llama-stack-configs/**/*.{yaml,yml}'",
    "verify": "prettier --check 'images.yaml' 'lightspeed-core-configs/**/*.{yaml,yml}' 'llama-stack-configs/**/*.{yaml,yml}'"
  },
  "version": "0.1.0"
}
